name: Hello World 
on:
  push:
    - main
    - feature/pu_pldemo
    - 'releases/**'
  pull_request:
    - main
    - feature/pu_pldemo
    - 'releases/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build the job
        run: echo "build the job"
