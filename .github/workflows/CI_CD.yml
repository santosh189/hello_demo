name: CI_CD demo
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-slim
    steps:
      - name: CI CD DEMO
        run: echo "build the job"
  test:
    runs-on: ubuntu-slim
    steps:
      - name: CI CD DEMO
        run: echo "unit test cases"
  deploy:
    runs-on: ubuntu-slim
    steps:
      - name: CI CD DEMO
        run: echo "deploy the application"
