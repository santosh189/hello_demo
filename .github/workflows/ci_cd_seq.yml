name: cicd_sequential_trigger_job
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build the job
        run: echo "build the job"
  test:
    runs-on: ubuntu-latest
    needs: build 
    steps:
      - name: test the job
        run: echo "unit test cases"
  deploy:
    runs-on: ubuntu-slim 
    needs: test 
    steps:
      - name: deploy 
        run: echo "deploy the application"
